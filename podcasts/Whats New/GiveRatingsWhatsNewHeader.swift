import SwiftUI

struct GiveRatingsWhatsNewHeader: View {
    var body: some View {
        Text("Stars Placeholder")
            .frame(height: 40.0)
            .background(.red)
            .foregroundStyle(.white).padding()
    }
}

#Preview {
    GiveRatingsWhatsNewHeader()
}
