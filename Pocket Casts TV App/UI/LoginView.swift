import SwiftUI

struct LoginView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Login")
            Spacer()
        }
    }
}

#Preview {
    LoginView()
}
