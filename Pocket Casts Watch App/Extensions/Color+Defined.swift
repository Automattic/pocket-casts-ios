import SwiftUI

extension Color {
    static let selectedBackground = Color.white.opacity(0.3)
    static let background = Color(UIColor(hex: "#202020"))
    static let lightGray = Color(UIColor.lightGray)
    static let subheadlineText = Color.white.opacity(0.5)
}
